import React from 'react';
import { connect } from 'react-redux';
import { removeProduct } from '../../ducks/cartDuck';
import Product from '../Product/Product';
import Shop from '..components/Shop/Shop';

const Cart = React.createClass({
  render() {
    return (
      <div>

    )
  }
})
